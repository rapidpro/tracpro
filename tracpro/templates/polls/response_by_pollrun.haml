- extends "smartmin/list.html"
- load smartmin i18n

- block pre-content

  {% include 'polls/pollrun_header.haml' with pollrun=pollrun %}

  .clearfix
    - if request.region and not pollrun.region
      - blocktrans with region=request.region
        These are the responses for <strong>{{ region }}</strong>

    .buttons.pull-away
      %a.btn.btn-default{ href:"{{ request.path }}?_format=csv" }
        %span.glyphicon.glyphicon-download
        - trans "Download"
